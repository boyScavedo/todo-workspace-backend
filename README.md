# For development

- Open terminal inside the `backend/` folder and then perform the following command
  ```
  npm install
  npm run dev
  ```
  > The command will return error if something else is running at PORT:5001.
- If no errors then open the [this link](http://localhost:5001/api-docs)
